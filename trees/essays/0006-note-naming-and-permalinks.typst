#import "../_lib/kodama.typ": *

#metadata((
  "title": "笔记命名与恒链接",
  "page-title": "笔记命名与恒链接",
  date: "2026 年 9 月 22 日",
  author: local("/about/hopo/index", text: "燎原火魄"),
  status: "draft",
  asref: true
))

总的来说，这里的命名不仅是给文本选定标题，也是给笔记文件命名，这就分为以下几种倾向：

- #embed("./0006/semantic-naming", "语义化命名", numbering: true)

- #embed("./0006/numbering-system", "编号系统", numbering: true)
  - 顺带一提，因为 0 - Z 的空位置太多，所以只需要单调递增而不需要特定含义时，笔者在心里认为自己选用了长度为 4， 从 0 - F（亦即 16 进制）的编号系统（其实随时可以升级）。这种情况下仍有 $16^4 = 65536$ 种可能，我想容纳笔者的创作力是远不成问题了。

- #embed("./0006/random-id", "随机标识符", numbering: true)

其中，笔者更青睐语义化命名和编号系统的组合。命名一向不是我的长项，更何况，要在创作之初就找到一个既准确概括文章的语义化命名，又要保持作为文件名的 slug 短小精悍，简直就是鱼和熊掌不可兼得。使用编号系统可以有效缓解此问题，又利于查找引用与原子化笔记（无论是扁平化还是保持一定层级结构）。笔者本希望提供只使用编号的链接作为恒链接，语义化命名则作为别名，但尚未研究清楚如何实践为好，因而选择了现在编号 + 可选语义命名的方案。如果上述期望得以实现，则现有的处在层级结构内的文本可以轻易取得唯一编号而扁平化（尽管会不连续，但照映了原子性？另一个方案是保留第一位作为该主题下的计数位）。
